# Test Database Configuration
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/medifast_test?schema=public"

# JWT Configuration for Tests
JWT_SECRET="medifast_jwt_test_secret_key_2024_super_secure"
JWT_EXPIRES_IN="1h"

# Server Configuration for Tests
PORT=3003
NODE_ENV="test"

# Upload Configuration for Tests
UPLOAD_DIR="uploads/test"
MAX_FILE_SIZE="5242880"

# Email Configuration for Tests (disabled)
EMAIL_FROM="test@medifast.com"
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""

# Mobile Sync Configuration for Tests
SYNC_BATCH_SIZE="50"
SYNC_TIMEOUT="10000"

# Security Configuration for Tests
BCRYPT_ROUNDS="4"
SESSION_TIMEOUT="1h"

# Test specific configurations
TEST_DB_RESET="true"
TEST_SEED_DATA="true"
TEST_LOG_LEVEL="error"